// Jewels and Stones

/**
 * @param {string} jewels
 * @param {string} stones
 * @return {number}
 */
var numJewelsInStones = function(jewels, stones) {
    var count = 0
    for(let jew of jewels){
        for(let stone of stones) if(stone == jew) count++;
    }
    return count
};
